export function createButtonSubscribe() {
  const btnSubscribe = document.createElement('button');
  btnSubscribe.classList.add('header-btn-subscribe');

  btnSubscribe.textContent = 'Записаться на прием';

  return btnSubscribe
}